FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      fvOptions;
}

heatSource
{
    type            scalarExplicitSource;
    active          yes;
    selectionMode   all;
    fields          (h);
    volumeMode      absolute;
    injectionRateSuSp
    {
        h           (25 0);
    }
}
