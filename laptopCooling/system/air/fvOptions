FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      fvOptions;
}

// 在 thermo.correct() 之前钳制焓场, 防止负温度致命错误
limitH
{
    type            limitTemperature;
    active          yes;
    selectionMode   all;
    min             250;
    max             450;
}
